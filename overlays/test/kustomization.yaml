apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../config/default

images:
- name: controller
  newName: docker.io/tenaghirmb/cronrestart
  newTag: v2.0.0-rc

patches:
- path: patches/deployment_patch.yaml
  target:
    kind: Deployment
